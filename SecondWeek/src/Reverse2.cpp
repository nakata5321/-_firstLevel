#include <iostream>
#include <vector>

using namespace std;

/*vector<int> Reversed(const vector<int>& v){
	vector<int>stp = v;
	for(int i = 0; i < stp.size(); i++){
		stp[stp.size()-(1+i)] = v[i];
	}
	return stp;
}

int main(){
	vector<int> numbers = {1, 5, 3, 4, 2};
	vector<int> revers = Reversed(numbers);
	for(int& s : revers)
		cout<< s;
 	return 0;
}*/
