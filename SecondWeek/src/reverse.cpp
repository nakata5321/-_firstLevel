#include <iostream>
#include <vector>
#include<string>

using namespace std;

/*void Reverse(vector<int>& v){
	vector<int>stp = v;
	for(int i = 0; i < stp.size(); i++){
		v[i] = stp[stp.size()-(1+i)];
	}
}*/

/*int main(){
	vector<int> numbers = {1, 5, 3, 4, 2};
	Reverse(numbers);
	for(int& s : numbers)
		cout<< s;
	cout<< numbers[2];
 	return 0;
}

*/
