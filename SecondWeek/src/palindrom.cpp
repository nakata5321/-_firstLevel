#include <iostream>
#include<string>

using namespace std;

/*bool IsPalindrom(string x){
	string y = "";
	for(int i = 0; i < x.length(); i++){
		y = y + x[x.length()-1-i];
	}
	if(x == y)
		return true;
	else
		return false;
}

int main() {
	string word;
	getline(cin, word);
	cout<<IsPalindrom(word);
	return 0;
}*/
