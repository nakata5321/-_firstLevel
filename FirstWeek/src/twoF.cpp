#include<iostream>
#include <string>

using namespace std;


/*int main(){
	string line;
	int count=0;
	int currentF=0;
	cin>>line;
	for(int i = 0 ; i < line.size(); i++){
		if(line[i] == 'f'){
			count++;
			currentF = i;
			if(count == 2)
				break;
		}
	}
	if(count > 0){
		if(count == 2)
			cout<<currentF;
		else
			cout<<-1;
	}
	else
		cout<<-2;

	return 0;
}*/
